name: php basic website ci workflow
on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

  schedule:
    - cron: '10 14 * * 0'
  workflow_dispatch:

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: setting up php
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - name: verifying user name and email
        run: |
          # correct git config syntax (no colon after key)
          git config --global user.name "akshattrentiums"
          git config --global user.email "akshatg@trentiums.net"
